{
  "name": "time-schedule-app",
  "version": "1.0.0",
  "description": "Static time schedule web application using localStorage",
  "scripts": {
    "start": "npx serve public"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
